node_modules
.next
out
.vercel
*.tsbuildinfo
next-env.d.ts
coverage
test-results
playwright-report
.data
pmdl
s.json
s2.json

# Secrets. NOT a blanket .env* : the Vercel CLI appends that on `vercel link`,
# and it would hide the committed .env.example that documents every variable.
.env
.env.local
.env.*.local

# Copied out of node_modules by scripts/copy-duckdb.mjs on every build.
public/duckdb
# Copied out of node_modules by scripts/copy-maplibre.mjs on every build.
public/maplibre

# Smoke-check output and throwaway patch scripts.
smoke-*.png
patch-*.py

# Written by `next dev` on every start. Local tooling instructions, not part of
# the deliverable, and recreated whenever the dev server runs.
AGENTS.md
CLAUDE.md
